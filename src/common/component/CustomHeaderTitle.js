import {Text} from 'react-native';
import React from 'react';

const CustomHeaderTitle = ({title}) => (
  <Text style={{fontSize: 21, color: 'black', marginLeft: 15}}>{title}</Text>
);

export default CustomHeaderTitle;
